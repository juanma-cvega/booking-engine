# Maven configuration for CI builds
# Batch mode: non-interactive, less verbose output
--batch-mode

# No transfer progress: reduces log noise
--no-transfer-progress

# Fail fast on errors
--fail-fast

# Show errors
--errors
